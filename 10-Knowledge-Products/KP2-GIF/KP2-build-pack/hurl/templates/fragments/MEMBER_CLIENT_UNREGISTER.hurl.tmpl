
# Unregister the subsystem -- reversal of MEMBER_CLIENT_REGISTER.hurl.tmpl
# (join-c plan Task 2). Raises a CLIENT_DELETION_REQUEST on the Central
# Server. CONFIRMED LIVE (apps/join-api/tests/fixtures/xroad/
# unjoin.client_unregister.json, unjoin.cs_deletion_approval_refused.json,
# docs/decisions/xroad-770-notes.md #11 finding 1): 204 on the first attempt, and
# UNLIKE registration there is no approval round to poll -- design decision
# 3's assumption of a symmetric approval gate is refuted: POST
# /management-requests/{id}/approval against the resulting
# CLIENT_DELETION_REQUEST returns 403 (it auto-processes). Do not add an
# approval poll here.
PUT https://{{@HOSTVAR@}}:4000/api/v1/clients/{{@CAP_P@_client_id}}/unregister
X-XSRF-TOKEN: {{@SESS_P@_xsrf_token}}

HTTP 204
