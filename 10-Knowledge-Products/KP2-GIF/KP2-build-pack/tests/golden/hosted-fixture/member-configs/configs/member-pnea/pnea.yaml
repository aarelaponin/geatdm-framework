# PNEA registration (consumer). Generated by bb-config-gen (prompts/register-member.md).
# Do not hand-edit — regenerate. Spec: NIIS X-Road member/subsystem registration.
#
# FIXTURE COPY -- tests/golden/hosted-fixture/ only, not the real
# configs/member-pnea/pnea.yaml. hosted_on below is the one line this copy
# adds (Wave 3 Task 5, S-05/Step 2): PNEA is a consumer that publishes no
# service (see docs/decisions/topology-profile-decision.md §1.3 -- "may be hosted" is
# G2's own verdict for exactly this shape of member), hosted on ss-plr the
# same way PHTB was in the real join-c run (docs/production-delta.md). This
# is what exercises hurl/generate.py's resolve_hosted_on_map() explicit-
# hosted_on branch, which no real canonical member config sets today (D5:
# one topology, nothing preset-hosted) and no other test in this suite
# reaches.
module: "pnea"
building_block: member-pnea
# member/subsystem identity comes from manifest.yaml's identity.members.pnea
# now -- restating it here was a second, driftable copy of the same values.
# Dogfooded 2026-07-26.
security_server:
  code: SS-PNEA
  dns_name: ss-pnea
  hosted_on: ss-plr
client:
  connection_type: HTTP        # demo only — production: HTTPS + client TLS certificate
services: []                   # consumer in v0.1; provides nothing
consumes:                      # the exchange this member exists for (wired in 2.6)
  - PROGRESSA/GOV/PNIA/IDENTITY/identity-api
  - PROGRESSA/GOV/PLR/ENROLMENT/enrolment-api
