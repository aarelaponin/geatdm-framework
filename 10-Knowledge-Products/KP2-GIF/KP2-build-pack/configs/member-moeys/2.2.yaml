# Module 2.2 — MoEYS/PEMIS registration. Generated by bb-config-gen (prompts/2.2.md).
# Do not hand-edit — regenerate. Spec: NIIS X-Road member/subsystem registration.
module: "2.2"
building_block: member-moeys
member:
  instance: PROGRESSA
  member_class: GOV
  member_code: MOEYS
  member_name: Progressa Ministry of Education, Youth and Sport
subsystem:
  code: PEMIS
  description: School information system (education management)
security_server:
  code: SS-MOEYS
  dns_name: ss-moeys          # lite profile: hosted on ss-plr instead
services:
  # type (always OPENAPI3), the forwarding URL (the SS reads servers.url from the
  # spec itself), enabled (generate.py always enables after publish) and
  # tls_verify are not config fields -- hurl/generate.py never reads them, so a
  # value here would be a second, driftable copy of what the deployment already
  # does. Dogfooded 2026-07-25.
  - code: pemis-api
    spec_url: http://app-pemis:8000/spec.yaml
    access: []                 # no consumers in v0.1; PEMIS is the future enrolment source
role_notes: provider (2.6 uses MOEYS:PEMIS as the unauthorised caller in the negative check)
