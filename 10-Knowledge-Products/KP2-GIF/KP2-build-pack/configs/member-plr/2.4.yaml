# Module 2.4 — PLR registration + enrolment service. Generated by bb-config-gen (prompts/2.4.md).
# Do not hand-edit — regenerate. Spec: NIIS X-Road subsystem + service description + access rights.
module: "2.4"
building_block: member-plr
member:
  instance: PROGRESSA
  member_class: GOV
  member_code: PLR
  member_name: Progressa Learner Registry
subsystem:
  code: ENROLMENT
  description: Authoritative learner enrolment register
security_server:
  code: SS-PLR
  dns_name: ss-plr
services:
  # type/forwarding URL/enabled/tls_verify: not config fields, see 2.2.yaml's
  # comment -- hurl/generate.py never reads them. Dogfooded 2026-07-25.
  - code: enrolment-api
    spec_url: http://app-plr:8000/spec.yaml
    access:                    # deliberate: PNEA only — MOEYS:PEMIS excluded (negative check 2.6)
      - PROGRESSA/GOV/PNEA/EXAMS
semantic:                      # from the Module 4 semantic map — 'enrolment' means:
  entity: enrolment
  key: nin
  fields: [nin, school, level, enrolment_year, status]
