# Module 2.5 — PNIA registration + identity service. Generated by bb-config-gen (prompts/2.5.md).
# Do not hand-edit — regenerate. Spec: NIIS X-Road subsystem + service description + access rights.
module: "2.5"
building_block: member-pnia
member:
  instance: PROGRESSA
  member_class: GOV
  member_code: PNIA
  member_name: Progressa National Identity Authority
subsystem:
  code: IDENTITY
  description: Authoritative person identity register
security_server:
  code: SS-PNIA
  dns_name: ss-pnia            # lite profile: hosted on ss-plr instead
services:
  # type/forwarding URL/enabled/tls_verify: not config fields, see 2.2.yaml's
  # comment -- hurl/generate.py never reads them. Dogfooded 2026-07-25.
  - code: identity-api
    spec_url: http://app-pnia:8000/spec.yaml
    access:                    # deliberate: PNEA only — MOEYS:PEMIS excluded (negative check 2.6)
      - PROGRESSA/GOV/PNEA/EXAMS
semantic:                      # from the Module 4 semantic map — 'person identity' means:
  entity: person
  key: nin
  fields: [nin, given_name, family_name, date_of_birth, sex, region]
