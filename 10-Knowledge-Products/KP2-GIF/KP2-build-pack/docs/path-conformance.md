# Conformance against the member onboarding path

<!-- GENERATED by scripts/render_path_conformance.py from
     docs/path-conformance.yaml. Do not edit this file: edit the YAML and
     re-render. tests/test_path_conformance.py fails if the two disagree, and
     fails if any cited evidence path does not exist. -->

**Path:** `08-Interoperability/GEATDM-Interop-Member-Onboarding-Path-v0.2.md` (v0.2 (rev. 4 Aug 2026))
**Last reviewed:** 2026-08-08

This is the only place the pack states its status against the path. Narrative
documents carry reasoning; where one of them also carries a status claim, treat
this file as authoritative — that divergence is what produced the corrections
dated 2026-08-08 in `docs/onboarding-alignment-design.md` and
`docs/onboarding-path-gap-analysis.md`.

**There is no tick mark here, deliberately.** A ✓ that meant "built" in one row
and "labelled" in the next is how a status table stops being readable. Four
statuses, and a clause that fits none of them is a defect rather than a fifth
status:

| Status | Means |
| --- | --- |
| **implemented** | A real artefact or check. If the pack broke it, something would fail. |
| **simulated** | Exercised for real, fidelity faked (Test CA, mock backends, synthetic data). Legitimate in a build pack; the declaration is part of the evidence. |
| **named absence** | Not built, and the pack says so where a reader following it will meet the absence. |
| **out of scope** | Deliberately not this pack's job, with a stated reason. |

## Summary

| Status | Clauses | Share |
| --- | ---: | ---: |
| implemented | 33 | 44% |
| simulated | 4 | 5% |
| named absence | 33 | 44% |
| out of scope | 4 | 5% |
| **total** | **74** | |

## Open defects

Absences the pack has not decided to accept — distinct from the named
absences below, which are deliberate and carry their lesson:

- **G5.9** — Exit test part 3 -- the response carries exactly the fields the contract declares

## §0 — What must exist before any member can be onboarded

| # | Clause | Status | Note | Evidence |
| --- | --- | --- | --- | --- |
| P0.1 | Central Server operating | **implemented** |  | `hurl/scenarios/00-cs-init.hurl`<br>`docker-compose.yml` (`central-server`) |
| P0.2 | Certification Authority | **simulated** | A Test CA signs any CSR it is handed, with no identity vetting. Real for the purpose of proving the certificate steps; not a trust service. | `hurl/scenarios/01-cs-trust-services.hurl`<br>`docs/production-delta.md` (`Test CA`) |
| P0.3 | Time-Stamping Authority | **simulated** |  | `hurl/templates/fragments/SS_TSA_POST.hurl.tmpl`<br>`docs/production-delta.md` (`Test CA`) |
| P0.4 | Member classes defined | **implemented** | Single-class federation (GOV), asserted consistent between the join policy and the manifest at generate time. | `configs/x-road-bus/join-policy.yaml` (`member_class`)<br>`hurl/generate.py` (`check_join_policy`) |
| P0.5 | Identifier and naming conventions published | **implemented** | Published as prose; only the identifier character set is enforced in code. The conventions-as-data file this was once planned as was not built, and was deliberately reversed (one rule, one place, no indirection). | `docs/conventions.md`<br>`apps/join-api/validate.py` (`_check_identifier_characters`) |
| P0.6 | Building-block pattern register | **named absence** | An ExchangePattern enum exists and both providers set a value, but there is no register of recognised patterns and nothing validates the value. Tier 1 of the semantic layer is therefore classification without a vocabulary. | `apps/join-api/writer.py` (`never resolved`)<br>`docs/onboarding-alignment-design.md` (`BB pattern register`) |

## §1 — The two-track shape

| # | Clause | Status | Note | Evidence |
| --- | --- | --- | --- | --- |
| P1.1 | A development track distinct from the production track | **named absence** | There is one track. The pack self-identifies as an instance of the development track rather than implementing the distinction. | `docs/production-delta.md` (`development track`) |
| P1.2 | Operator obligation: the development environment is genuinely separate -- its own Central Server, CA/TSA and trust anchor | **named absence** | Unmet by construction: one Central Server, one CA, one TSA, one anchor. The prohibition on real personal data is enforced by authorship, not by membership terms. | `docs/production-delta.md` (`development track`)<br>`configs/x-road-bus/federation-core.yaml` |

## §1a — Conventions to publish before G1

| # | Clause | Status | Note | Evidence |
| --- | --- | --- | --- | --- |
| P1a.1 | Identifier character set (X-Road 7.3+ allowlist, XRDDEV-1960) | **implemented** | Positive allowlist match, rejection message names the permitted set. | `apps/join-api/validate.py` (`XRDDEV-1960`)<br>`docs/conventions.md` |
| P1a.2 | Member code scheme | **implemented** | Published; collision-checked against existing members at join time. | `docs/conventions.md`<br>`apps/join-api/validate.py` (`_check_collision`) |
| P1a.3 | Subsystem code scheme (one per system, not per service) | **implemented** |  | `docs/conventions.md`<br>`apps/join-api/schema.py` (`subsystem`) |
| P1a.4 | Security Server host naming convention | **named absence** | The pack applies ss-<key> consistently but does not check it, and the convention encodes neither role nor environment nor sequence -- so it cannot distinguish a development server from a production one, which is what the path's example convention exists to do. | `docs/conventions.md` |

## G0 — Eligibility and intent

| # | Clause | Status | Note | Evidence |
| --- | --- | --- | --- | --- |
| G0.1 | Eligibility test against the ecosystem's admission policy | **named absence** |  | `apps/join-api/writer.py` (`Application (G0)`)<br>`onboarding/plr/00-gates.md` (`not implemented in this demo`) |
| G0.2 | Membership agreement signed by the member's senior representative | **named absence** | Named in every member's gate register. The path's separation of the membership agreement from the Member Requirements is reflected: the Requirements half is built. | `apps/join-api/writer.py` (`signed membership agreement`)<br>`onboarding/plr/02-requirements.md` |
| G0.3 | Member names a Technical Focal Point and, where personal data flows, a DPO | **named absence** | A free-text technical_contact is required and rendered. There is no DPO field and no role semantics on the contact -- the absence is not currently named anywhere a reader would meet it. | `apps/join-api/schema.py` (`technical_contact`)<br>`onboarding/plr/02-requirements.md` |
| G0.4 | Exit test -- does the applicant hold a legal mandate for data it proposes to expose as authoritative? | **named absence** | Not testable. _check_lawful_basis returns early when the payload declares services, so the requirement applies only to consumer-only members -- the inverse of the applicant class the exit test exists to catch. Neither canonical provider states a lawful basis for the data it publishes. | `apps/join-api/validate.py` (`_check_lawful_basis`)<br>`configs/member-plr/plr.yaml` (`lawful_basis`) |

## G1 — Admission decision

| # | Clause | Status | Note | Evidence |
| --- | --- | --- | --- | --- |
| G1.1 | The Steering Committee decides admission; the Operating Authority recommends | **out of scope** | In both reference instantiations the decision is taken outside any system (Estonia: a RIHA registry record; Finland: a form to DVV), and path 4 states G0-G3 are "not automatable, and should not be". Building a committee role would model a control no X-Road operator has. What is in scope is the coupling -- a technical join that cannot complete without a reference to the decision -- which is G1.2. | `onboarding/plr/00-gates.md` (`Steering Committee`) |
| G1.2 | The technical join is coupled to the admission decision | **implemented** | POST /approve refuses without a decision_reference and rejects the applicant token outright. The reference is checked for non-emptiness only, and is not yet written into a per-member admission record -- so the coupling exists at the API boundary but leaves no gate-exit artefact. | `apps/join-api/app.py` (`decision_reference`)<br>`apps/join-api/app.py` (`require_operator`) |
| G1.3 | Exit test -- the allocated identity is unique and legal | **implemented** | Both halves. Uniqueness across directory, manifest, DNS name and Security Server code; legality against the 7.3+ allowlist. | `apps/join-api/validate.py` (`_check_collision`)<br>`apps/join-api/validate.py` (`_bad_identifier`) |

## G2 — Hosting and topology decision

| # | Clause | Status | Note | Evidence |
| --- | --- | --- | --- | --- |
| G2.1 | Decide own Security Server vs hosted as a client on another's | **implemented** | Both shapes are built and both have been run live. Hosting is the default for a joined member; hosting chains are rejected. | `apps/join-api/validate.py` (`_check_hosting`)<br>`configs/x-road-bus/join-policy.yaml` (`default_hosting`)<br>`apps/join-api/job.py` (`build_sequence`) |
| G2.2 | The host's token holds the joined member's signing key -- a delegation with no counterpart in the obligation set | **implemented** | Implemented exactly as the path describes, including the consequence: the SIGN key id must be correlated by certificate owner_id, never by label, because a shared token carries one identically-labelled key per hosted member. The delegation is not recorded in the member's onboarding record. | `apps/join-api/job.py` (`_sign_key_id`)<br>`hurl/generate.py` (`build_hosted_client`) |
| G2.3 | Exit test -- is the hosting choice compatible with the member's role? An authoritative personal-data publisher is not hosted on a peer's server | **named absence** | Enforced nowhere. The reasoning exists as a design decision (it is why the canonical topology gives every member its own server rather than hosting the identity register on a peer), but a submitted payload that hosts an authoritative provider on a peer validates today. | `docs/onboarding-alignment-design.md` (`violates G2`) |
| G2.4 | Publish minimum Security Server sizing so procurement is not a negotiation | **named absence** | The pack publishes measured demo figures (per-server RAM at steady state, container counts) but no minimum production dimensioning of the kind the path's Finland example gives. | `README.md` (`GiB`) |

## G3 — Identity and trust

| # | Clause | Status | Note | Evidence |
| --- | --- | --- | --- | --- |
| G3.1 | The CA issues member-grade certificates; own-server gets auth and sign, hosted gets sign only | **simulated** | The asymmetry is real -- a hosted member never generates an auth key. The issuance is not: the Test CA signs any CSR presented. | `apps/join-api/job.py` (`ss.auth_key_csr`)<br>`docs/production-delta.md` (`Test CA`) |
| G3.2 | Exit test -- the chain validates from the member's own environment | **simulated** | Proxied rather than tested: an OCSP marker in the reachability call detects stale Test-CA revocation state from the caller's side, which is the same failure surface but not the same assertion. | `apps/join-api/job.py` (`OCSP_MARKER`) |
| G3.3 | Trust services are a third party; their SLA sets G3's duration and belongs in the risk register | **out of scope** | A demo cannot have a third-party trust-service dependency. The path's own remedy is a risk-register row in TK-IO-13, which is a framework artefact, not a pack artefact. | `docs/production-delta.md` (`Test CA`) |

## G4 — Platform conformance

| # | Clause | Status | Note | Evidence |
| --- | --- | --- | --- | --- |
| G4.1 | Install, register with the Central Server, obtain and activate certificates, configure timestamping | **implemented** | Path steps 1-6, in order, driven over the admin API against a live server. | `apps/join-api/job.py` (`build_sequence`)<br>`hurl/steps.py` (`REGISTRY`) |
| G4.2 | Network and firewall -- publish the ecosystem's message ports | **implemented** |  | `docs/production-delta.md` (`5500`) |
| G4.3 | Host naming per 1a | **named absence** | See P1a.4 -- applied by convention, not checked, and carries no environment field. | `docs/conventions.md` |
| G4.4 | Monitoring add-ons installed on the Security Server at G4 | **implemented** | Both add-ons run on every Security Server, and a regression test refuses any server that resolves to a slim Sidecar image without them. This is the asymmetric-cost item the path names: trivial at G4, a campaign later. | `tests/test_addons.py` (`test_every_security_server_uses_the_full_non_slim_sidecar_image`)<br>`acceptance/member.md` (`xroad-opmonitor`) |
| G4.5 | Configuration tooling rather than bespoke scripts | **implemented** | Config-as-code over the sanctioned admin API rather than hand configuration, which is the property the path's recommendation protects. The pack generates its own scenarios rather than using the NIIS Security Server Toolkit or the Ansible playbooks the path names. | `hurl/generate.py`<br>`hurl/README.md` |
| G4.6 | Exit test part 1 -- the server appears in global configuration as registered and active | **implemented** |  | `acceptance/join-member.md` (`REGISTERED`)<br>`apps/join-api/job.py` (`_probe_auth_cert_registered`) |
| G4.7 | Exit test part 2 -- the conformance suite passes without waivers | **implemented** |  | `scripts/acceptance.sh`<br>`scripts/verify.sh` |
| G4.8 | Exit test part 3 -- is its monitoring data arriving centrally? | **named absence** | Add-ons emit to no collector. Deliberate: an installed add-on with a declared absent collector is the pack's counter-example for the path's own point, and adding X-Road Metrics later closes it without a retrofit. | `docs/onboarding-alignment-design.md` (`remains unmet`) |

## G5 — Service conformance and registration

| # | Clause | Status | Note | Evidence |
| --- | --- | --- | --- | --- |
| G5.1 | Artefact 1 -- the service contract (OpenAPI), submitted to the OA | **implemented** | Fetched and parsed at validation time, not merely declared; operations are screened against the join policy's allowed methods. The contract is referenced by URL and is not copied into the onboarding record. | `apps/join-api/validate.py` (`_check_backend_reachability`)<br>`apps/join-api/validate.py` (`_check_allowed_methods`) |
| G5.2 | Artefact 2 tier 1 -- which GovStack building-block pattern this exchange instantiates | **named absence** | Classified on the payload, validated against nothing. See P0.6. | `apps/join-api/schema.py` (`ExchangePattern`)<br>`apps/join-api/writer.py` (`never resolved`) |
| G5.3 | Artefact 2 tier 2 -- the member's fields mapped onto the published sector vocabulary | **implemented** | Conformance, not presence: the entity must be a key in the published map and every declared field must be declared for that entity. The mapping is not written into the onboarding record. | `apps/join-api/validate.py` (`_check_purpose_limitation`)<br>`configs/semantic/semantic-map.yaml` |
| G5.4 | Artefact 3 -- the SLA, signed | **implemented** | A published service without an SLA is rejected; one signed record per service is rendered into the member's onboarding folder. The values are illustrative, and the record is attached to the member rather than discoverable with the service. | `apps/join-api/validate.py` (`_check_sla_required`)<br>`onboarding/plr/03-sla/enrolment-api.md` |
| G5.5 | Artefact 4 -- the access-control list, exactly the subjects the policy names | **implemented** | Every subject resolved against the manifest; self-grants rejected; applied one entry per subject. | `apps/join-api/validate.py` (`_check_acl_sanity`)<br>`onboarding/plr/05-registration.md` |
| G5.6 | Register -- publish the description, apply the ACL, create the service catalogue entry | **named absence** | Publication and ACL are implemented. There is no catalogue, so the SLA lands on the member record rather than as catalogue metadata -- a smaller version of the orphan-SLA problem the path's G5 addition sets out to fix. | `docs/onboarding-alignment-design.md` (`Service catalogue entry`) |
| G5.7 | Exit test part 1 -- an authorised consumer's call reaches the backend end to end | **implemented** | Asserted per joined service at registration time, with its own retry budget; any X-Road fault counts as unverified. | `apps/join-api/job.py` (`_default_r1_call`)<br>`apps/join-api/job.py` (`R1_RETRY_BUDGET`) |
| G5.8 | Exit test part 2 -- an unauthorised subsystem's identical call is denied by the provider-side access control, not by a transport error | **implemented** | The clause the path calls the one usually skipped, and the pack does not skip it: the negative call is routed through the Security Server that hosts the provider, so the denial cannot come from the caller's own server rejecting an unknown client, and the specific X-Road access-denied fault is required rather than any failure. | `scripts/acceptance.sh` (`check_264`)<br>`scripts/acceptance.sh` (`check_r1_denied`)<br>`configs/x-road-bus/once-only-exchange.yaml` (`unauthorised_client`) |
| G5.9 | Exit test part 3 -- the response carries exactly the fields the contract declares | **named absence** | OPEN DEFECT, not a deliberate deferral. Nothing compares a live response against the OpenAPI contract. The property currently holds by construction because the mock provider derives its own output from the same contract, and the nearest assertion compares returned values to the seed row field by field -- so a response that drops a required field, or that adds a field the CSV carries and the contract withholds, passes silently. This is the check that stops being free when a real backend replaces the mock. | `apps/mock-registry/app.py` (`DECLARED_FIELDS`)<br>`scripts/assert_record.py` |
| G5.10 | Adapter service for a legacy backend that cannot expose a conformant interface | **out of scope** | Every backend here is a mock built to the contract, so the pattern has nothing to adapt. It gets its first real test when real applications arrive behind the same contracts. | `docs/onboarding-path-gap-analysis.md` (`adapter`) |

## G6 — Production go-live and handover

| # | Clause | Status | Note | Evidence |
| --- | --- | --- | --- | --- |
| G6.1 | Production go-live and handover; the member's monitoring sees what the OA's sees | **named absence** | Depends on G4.8; a demo has no production go-live to hand over. | `apps/join-api/writer.py` (`Go-live (G6)`)<br>`onboarding/plr/00-gates.md` (`Go-live`) |

## GX — Retirement

| # | Clause | Status | Note | Evidence |
| --- | --- | --- | --- | --- |
| GX.1 | Revoke ACLs, unregister services, unregister the subsystem, delete the client, remove the member from the Central Server | **implemented** | A reversal order established live rather than mirrored from the forward sequence -- a naive mirror deletes the signing key before the client that owns it. Every step is probed for absence first, with a per-step reading of what absence looks like. | `hurl/steps.py` (`REVERSAL_ORDER`)<br>`apps/join-api/job.py` (`def unjoin`)<br>`acceptance/join-member.md` (`RETIRED`) |
| GX.2 | Revoke ACLs naming the departing member as a subject on another member's service | **named absence** | The reversal revokes the departing member's own service ACLs only. Unreachable today because no joined member holds an inbound grant; it becomes reachable the moment one consumes, and the gap is named beside the reversal order so whoever adds the grant path finds it. | `hurl/steps.py` (`AS A SUBJECT`) |
| GX.3 | Revoke certificates; notify every consumer that held access; remove the catalogue entry | **named absence** | No CA revocation call exists, no consumer notification, and there is no catalogue to remove an entry from. For an own-server member the cleanup is handed to the operator as instructions rather than executed. | `apps/join-api/job.py` (`retire_instruction`) |
| GX.4 | Exit test -- the member is absent everywhere AND its message-log records remain retrievable for the statutory retention period | **named absence** | The absence half is asserted, including from the shared host's token with every other member's key still present. The retention half is inverted: teardown deletes the archive volume, with the consequence stated in prose beside the instruction rather than prevented. | `acceptance/join-member.md` (`RETIRED`)<br>`runbook.md` (`retention`) |

## §3 — The technical sequence inside G4–G5

| # | Clause | Status | Note | Evidence |
| --- | --- | --- | --- | --- |
| S3.1 | Steps 1-6 (own-server) in the stated order, with the stated actors | **implemented** | Actor attribution is preserved per step and drives a real wait state: an own-server join blocks before the first member step until the member's server answers. | `hurl/steps.py` (`REGISTRY`)<br>`apps/join-api/job.py` (`BLOCKED_POLL_ATTEMPTS`) |
| S3.2 | Steps 7-11 for both shapes, with client-add before signing-key generation before registration | **implemented** | The order the path calls load-bearing, and the code documents why: the signer rejects a member id it does not yet recognise as a client. | `hurl/steps.py` (`client_not_found`)<br>`apps/join-api/job.py` (`build_sequence`) |
| S3.3 | Step 12 -- verify authorised call reaches the backend, unauthorised call denied by the ACL | **implemented** | Split across two places: the positive half runs inside the join, the negative half in the acceptance suite. See G5.7 and G5.8; the field conformance clause is G5.9 and is open. | `apps/join-api/job.py` (`_default_r1_call`)<br>`scripts/acceptance.sh` (`check_r1_denied`) |
| S3.4 | Version fact 1 -- approval of registration requests is an operator policy choice, automatic or manual, since 6.21.0 | **named absence** | Only the manual policy is implemented. The join policy declares `approval: explicit` and the key is whitelisted, but its value is never read -- setting it to automatic changes nothing. By the pack's own rule that a key nothing applies is decoration, this key is decoration. | `configs/x-road-bus/join-policy.yaml` (`approval`)<br>`hurl/generate.py` (`JOIN_POLICY_KEYS`) |
| S3.5 | Version fact 2 -- no complementary requests since 7.3.0 | **implemented** | Registration is submitted from the Security Server and approved on the Central Server; no complementary request is created anywhere. | `hurl/steps.py` (`ss.client_register`)<br>`docs/xroad-770-notes.md` (`management-requests`) |
| S3.6 | Version fact 3 -- the management API is the sanctioned integration point | **implemented** | Session-authenticated admin REST API throughout; no UI scripting anywhere. | `apps/console/xroad.py` (`X-XSRF-TOKEN`)<br>`PLAN.md` (`/api/v1`) |

## §4 — What can be automated, and what cannot

| # | Clause | Status | Note | Evidence |
| --- | --- | --- | --- | --- |
| S4.1 | G4-G5 technical work is fully automatable via the management APIs, in minutes | **implemented** | The pack's central claim, and the one it demonstrably supports. Measured: a hosted join reaches active and verified in well under two minutes; an own-server join in roughly two to three minutes after its server is up, plus a real blocked wait while it stands up. | `README.md` (`two minutes`)<br>`acceptance/join-member.md` |
| S4.2 | G0-G3 are organisational and should not be automated | **out of scope** | Agreed with rather than worked around. The pack's G0-G3 absences are the path's recommendation applied, not a shortfall -- with the exception of G0.4, where the absence removes a test the path does want somewhere. | `onboarding/plr/00-gates.md` (`not implemented in this demo`) |

## §6 — The operator's own building blocks

| # | Clause | Status | Note | Evidence |
| --- | --- | --- | --- | --- |
| S6.1 | Member management / service management portal | **implemented** | Demo-grade, and the path says this is the one operator building block with no open-source starting point -- so a demo-grade version is the honest maximum here. A join API plus a one-page console, not a self-service portal. | `apps/join-api/app.py`<br>`apps/console/app.py` |
| S6.2 | Service catalogue | **named absence** |  | `docs/onboarding-alignment-design.md` (`Service catalogue`) |
| S6.3 | Reporting and metrics | **named absence** | Add-on installed at G4, no collection layer. See G4.8. | `docs/onboarding-alignment-design.md` (`remains unmet`) |
| S6.4 | Technical monitoring | **named absence** | As S6.3 -- environmental-monitoring add-on installed, nothing collects from it. | `tests/test_addons.py` (`xroad-monitor`) |

## §6a — Building blocks and the semantic layer

| # | Clause | Status | Note | Evidence |
| --- | --- | --- | --- | --- |
| S6a.1 | Tier 1 -- BB pattern, cross-sector | **named absence** | See P0.6 and G5.2. | `apps/join-api/schema.py` (`ExchangePattern`) |
| S6a.2 | Tier 2 -- sector entity anchored in sector standards | **implemented** |  | `configs/semantic/semantic-map.yaml` (`OneRoster`)<br>`apps/join-api/validate.py` (`_check_purpose_limitation`) |
| S6a.3 | Tier 3 -- the member's own contract, checked at G5 | **implemented** |  | `configs/member-plr/plr.yaml` (`semantic`)<br>`apps/specs/plr-enrolment.openapi.yaml` |
| S6a.4 | The catalogue entry carries the tier-1 classification so services are discoverable by pattern | **named absence** | Depends on S6.2. Neither tier reaches the onboarding record either. | `apps/join-api/writer.py` (`render_onboarding_tree`) |

## §7 — The onboarding file

| # | Clause | Status | Note | Evidence |
| --- | --- | --- | --- | --- |
| S7.1 | 00-application.md -- G0 application and signed membership agreement | **named absence** |  | `onboarding/plr/00-gates.md` (`Application (G0)`) |
| S7.2 | 01-admission.md -- G1 admission decision and allocated identity | **named absence** | The API holds everything this file needs at approve time -- request id, decision reference, approving principal, timestamp -- and writes none of it. The cheapest remaining gate artefact. | `onboarding/plr/00-gates.md` (`Admission (G1)`)<br>`apps/join-api/app.py` (`decision_reference`) |
| S7.3 | 02-topology.md -- G2 hosting decision, reason, trust note, sizing | **named absence** | Not written, and not named in the gate register either -- the hosting decision survives only as one row of the registration record. | `onboarding/plr/05-registration.md` (`Hosting`) |
| S7.4 | 03-certificates.md -- G3 issuance record and member-side verification | **named absence** |  | `onboarding/plr/00-gates.md` (`Certificates (G3)`) |
| S7.5 | 04-platform-conformance.md -- G4 result, monitoring add-ons confirmed, host name, firewall record, waivers | **named absence** | Not written, and not named in the gate register. | `acceptance/member.md` (`xroad-opmonitor`) |
| S7.6 | 05-services/<code>/ -- contract, semantic map, SLA, ACL, catalogue entry | **named absence** | Two of five land, relocated: the SLA as 03-sla/<service>.md and the ACL subjects as a row of 05-registration.md. The contract, the semantic map and the catalogue entry are not written. | `onboarding/plr/03-sla/enrolment-api.md`<br>`onboarding/plr/05-registration.md` |
| S7.7 | 06-golive.md -- G6 handover and first-transaction monitoring | **named absence** |  | `onboarding/plr/00-gates.md` (`Go-live`) |
| S7.8 | 99-retirement.md -- written only at exit | **named absence** | Not written, and not named in the gate register, although GX is the most completely implemented gate in the pack -- and the record is deleted at retirement rather than closed out. | `scripts/member.sh` (`onboarding`) |
| S7.9 | Every file corresponds to a gate exit; a folder missing a file has not passed that gate | **implemented** | The rule is stated at the top of every member's gate register and the records are generated, never hand-maintained, so nothing backfills a plausible-looking stub. The pack builds four files where the path specifies ten, and the gate register names four of the six absences. | `apps/join-api/writer.py` (`render_onboarding_tree`)<br>`onboarding/plr/00-gates.md` (`has not been passed`) |

---

Regenerate with `python3 scripts/render_path_conformance.py`. `tests/test_path_conformance.py` proves every path above exists and every symbol resolves.
