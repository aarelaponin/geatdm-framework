#!/usr/bin/env bash
# Deploy KP2 configs onto the live stack. Reuse joget-deploy (KP4) / X-Road CLI (KP2)
# / the mtca-data-platform MDM load (KP3). Runs on the workstation against ITU cloud,
# NOT in the sandbox (see mtca-dev-workflow two-track method).
set -euo pipefail
echo "TODO: deploy configs/ for KP2"
