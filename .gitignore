# IDE
.idea/
*.iml

# OS
.DS_Store
Thumbs.db

# Editor backup files
*~
*.swp
*.swo

# Archived snapshots — local-only, not tracked in git history
x_archive/

# Office lock files
~$*
~$*.docx

# Claude Code workspace config (user-specific)
.claude/

# Python bytecode
__pycache__/
*.pyc

# Superpowers subagent-driven-development scratch (ledgers, briefs, reports)
.superpowers/sdd/

# Superpowers isolated worktrees
.worktrees/

# Rendered video — regenerate from deck + audio + cues via kp-slidecast
*.mp4

# Per-host stems from the voice swap (Step 4b) — rebuildable from the take
*_Stem-*.wav

# Camera source + QA frame grabs — large, not versioned, live outside git
*.mov
**/video/_qa/
